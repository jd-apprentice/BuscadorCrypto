import Button from "react-bootstrap/Button";

const AgregarFavoritos = () => {
  return (
    <Button>
      <i className="bi bi-star-fill d-flex"></i>
    </Button>
  );
};

export default AgregarFavoritos;
